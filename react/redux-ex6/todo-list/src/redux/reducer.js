import {SET_TODO, DEL_TODO} from './action';const initState = [    {        id : 1,        text : 'Hello',    },    {        id : 2,        text : '??',    }]export const reducer = (state = initState,action) => {    const {type,payload} = action || {};    switch (type){        case SET_TODO: return state.concat(payload);        case DEL_TODO: return state.filter(todo => todo.id !== payload.id);        default : return initState;    }}