import {GET_TEXT} from "./action";const initState = {    text : '',}export const reducer = (state = initState,action) => {    const {payload,type} = action;    switch (type){        case GET_TEXT : return{            text : payload.text,        }        default : return initState;    }}