import {COMPARE_SIZE, GET_SIZE} from './action'export const initState = {    width: 0,    height: 0,}export const reducer = (state = initState, action) => {    const {type,payload} = action;    const {width,height} = payload || {}    switch (type) {        case GET_SIZE :            return {                width: width,                height: height            }        case COMPARE_SIZE:            return {                width: state.width - width,                height: state.height - height            }        default :            console.log("null action function");            return state    }}