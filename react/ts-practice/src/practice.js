"use strict";
var count = 0;
count += 1;
// count = "갑자기 분위기 문자열"; //이러면 에러가 납니다.
var message = 'hello world';
var done = true;
var numbers = [1, 2, 3];
var messages = ['hello', 'world'];
// message.push(1);// 숫자 넣으려고 하면 안된다.
var mightBeUndeFined = undefined; //string 일수도 있고 underfined 일수도 있다.
var nullableNumber = null;
var color = 'red';
color = 'yellow';
// color = 'green'; //에러 발생
