import React, {useState} from 'react';export default () => {    const [isToggle, handleClick] = useState(false);    return (        <div>            <button onClick={() => handleClick(!isToggle)}>{isToggle ? 'On' : 'OFF'}</button>        </div>    );    //eventhandler는 빼주는게 좋다.};