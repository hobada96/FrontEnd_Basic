import React, {useState,} from 'react'export default () => {    //선언    const [isLoggedIn, setLoggedIn] = useState(true)    //useCallback,    //UseEffect    //unmount => 무슨말인지 알거같다.    // 삼항연산자는 한줄로 쓰지 않는다.    return (        <div>            {isLoggedIn                ? "Singned In Please"                : "Welcome Back"}            <br/>            <button onClick={() => setLoggedIn(!isLoggedIn)}>{isLoggedIn ? "LogIn" : "Logout"}</button>        </div>    );}