import React, {useRef} from 'react';import {  StyleSheet,  Text,  Linking, Alert} from 'react-native';import QRCodeScanner, {Event} from 'react-native-qrcode-scanner';export default () => {  const ref = useRef(null);  const onPressNo = () => {    ref.current.reactivate();  };  const Prompt = (data: string) => {    const openURL = () => {      Linking.openURL(data).catch(err => console.error('An error occured', err));    };    const getYesObject = () => ({      text: '네',      onPress: openURL,    });    const getNoObject = () => ({      text: '아니오',      onPress: onPressNo    });    Alert.alert(      'Header',      '지정하신 ' + data + '을 실행하시겠습니까?',      [getYesObject(), getNoObject()]    );  };  const onReadHandler = ({e: {data}}: Event) => {    data && Prompt(data);  };  return (    <QRCodeScanner      ref={ref}      onRead={onReadHandler}      topContent={        <Text style={styles.centerText}>          <Text style={styles.textBold}>DB LAB 출석</Text>        </Text>      }      vibrate={false}    />  );};const styles = StyleSheet.create({  centerText:    {      flex: 1,      fontSize: 18,      padding: 32,      color: '#777'    },  textBold:    {      fontWeight: '500',      color: '#000'    },  buttonText:    {      fontSize: 21,      color: 'rgb(0,122,255)'    },  buttonTouchable:    {      padding: 16    }});