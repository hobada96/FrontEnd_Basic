import React, {Component, useEffect, useState} from 'react';import {  StyleSheet,  Text,  Linking} from 'react-native';import QRCodeScanner, {Event} from 'react-native-qrcode-scanner';//api call//web server -> springbootexport default () => {  const onSuccess = (e: Event) => {    Linking.openURL(e.data).catch(err => console.error('An error occured', err));  }// event Handler같은 경우 component내에서 실행되는 것이기 때문에 compoenet안에 있어야한다. event Handler  return (    <QRCodeScanner      onRead={(e) => onSuccess(e)}      topContent={        <Text style={styles.centerText}>          <Text style={styles.textBold}>DB LAB 출석</Text>        </Text>      }      reactivate={true}      reactivateTimeout={2500}      //사용성을 고려한다. 해당 URL로 이동하시겠습니까?    />  );};const styles = StyleSheet.create({  centerText:    {      flex: 1,      fontSize: 18,      padding: 32,      color: '#777'    },  textBold:    {      fontWeight: '500',      color: '#000'    },  buttonText:    {      fontSize: 21,      color: 'rgb(0,122,255)'    },  buttonTouchable:    {      padding: 16    }});