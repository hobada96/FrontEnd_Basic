function solution(n) {    let arr = Array(n).fill(null).map(()=>Array()); // 세로 가로    let count = 1;    [arr,count] = repeatRule(n,arr,count);    return arr;}const repeatRule = (n,arr,count) => {    for(let i=n-1;i>=0;i--){        arr[i][0] = count;        count ++;    }    for(let j=1;j<n;j++){        arr[0][j] = count;        count++;    }    for(let i=1;i<n-1;i++){        let j = i;        arr[i][j] = count;        count++;    }    return [arr,count]}console.log(solution(4));