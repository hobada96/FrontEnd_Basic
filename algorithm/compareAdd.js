function solution(numbers) {    let answer = [];    for(let i=0; i<numbers.length; i++){        add(numbers,i,answer);    }    const set = Array.from(new Set(answer));    return set.sort(compareNumbers);}const add = (a,b,answer) => {    for(let i=b; i<a.length-1; i++){        answer.push(a[b] + a[i+1])    }    return answer;}const compareNumbers = (a, b) => {    return a - b;}