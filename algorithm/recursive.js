function solution(s) {    return repeat(s.split(''),0,0);}const repeat = (arr,repeatCounts,zeroCounts) =>{    const repeatCount = repeatCounts + 1;    const zeroCount = arr.filter(x => x === '0').length;    const newArr = arr.filter(x=> x != 0).length.toString(2).split('');    if(arr == '1')return [repeatCounts,zeroCounts]    return repeat(newArr,repeatCount,zeroCount);}