function solution(next_student) {    let arr = [];    for(let i=1; i< next_student.length;i++){        arr.push(i)    }    let answer = new Array(next_student.length+1);    for (let i = 0; i < next_student.length+1; i++) {        answer[i] = new Array(null);    }    for(let i=2;i<next_student.length;i++){        answer[next_student[i]].push(i)    }    return answer    return arr.filter(z => !answer.filter((x,i)=>i !== 0).flatMap(x => x).filter(x => x != null).sort((a,b) => a - b).includes(z)).length}console.log((solution(	[5, 9, 13, 1, 0, 0, 11, 1, 7, 12, 9, 9, 2])))