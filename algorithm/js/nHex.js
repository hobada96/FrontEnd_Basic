function solution(n, t, m, p) {    const answer = [];    let i = 0;    let count = 0;    while(true){        const changeNum = i.toString(n);        if(answer.length === t) break;        for(let j=0;j<changeNum.length;j++){            if(answer.length === t) break;            count++;            if(count % m === (p%m)){                answer.push(changeNum[j].toUpperCase())            }        }        i++;    }    return answer.join('')}