function solution(skill, skill_trees) {    let count = 0;    const arr = [];    for(let i=0;i<skill_trees.length;i++){        arr.push(skill_trees[i].split('').filter(x => skill.includes(x)));    }    for(let i=0 ; i < arr.length;i++){        if(skill.indexOf(arr[i].join('')) == 0) count ++;    }    return count}// 숏코딩, 리팩토링const solution = (skill, skill_trees) =>    skill_trees.filter(skill_tree =>        skill.indexOf(            skill_tree.split('').filter(                y => skill.includes(y)            ).join('')) == 0).length