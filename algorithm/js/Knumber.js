//모범 답안.function solution(array, commands) {    return commands.map(v => {        return array.slice(v[0] - 1, v[1]).sort((a, b) => a - b).slice(v[2] - 1, v[2])[0];    });}// 내가 푼 것function solution(array, commands) {    return compare(array,commands);}const compare = (array,commands) => {    const return_arr = [];    for(let i =0;i<commands.length ;i++){        const arr = [];        for(let l=commands[i][0]-1;l<commands[i][1];l++){            arr.push(array[l]);        }        arr.sort((a,b)=>a-b);        return_arr.push(arr[commands[i][2]-1]);    }    return return_arr;}