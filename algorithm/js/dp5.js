const dp = (N,K) => {    const arr = Array.from(Array(201), () => Array(201).fill(0))    for (let n = 0; n <= N; n++) {        arr[1][n] = 1    }    for (let k = 2; k <= K; k++) {        for (let n = 0; n <= N; n++) {            for (let i = 0; i <= n; i++) {                arr[k][n] = arr[k][n] + arr[k - 1][i];            }            arr[k][n] = arr[k][n] % 10000000;        }    }    console.log(arr[K][N]);}dp(3, 4);